const slug=new URLSearchParams(location.search).get('slug');
const set=(id,value)=>{const node=document.querySelector(id);if(node)node.textContent=value||'À renseigner'};
const esc=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
const logo=(url,name)=>url?`<img class="match-logo" src="${esc(url)}" alt="Logo ${esc(name)}" loading="lazy" referrerpolicy="no-referrer">`:`<span class="match-logo match-logo--fallback" aria-hidden="true">${esc(String(name||'?').trim().charAt(0))}</span>`;
const roleLabels={coach_referent:'Coach référent',coach:'Coach',dirigeant:'Dirigeant',arbitre:'Arbitre'};
const api=resource=>fetch(`/api/${resource}`).then(async response=>{if(!response.ok)throw new Error(`${resource}: ${response.status}`);const data=await response.json();return Array.isArray(data)?data:[]});
Promise.all([api('teams'),api('training_sessions').catch(()=>[]),api('club_members').catch(()=>[]),api('team_staff').catch(()=>[]),api('venues').catch(()=>[]),api('matches').catch(()=>[]),api('seasons').catch(()=>[])]).then(([teams,sessions,members,assignments,venues,matches,seasons])=>{
  const team=teams.find(item=>item.slug===slug);if(!team){document.querySelector('#team-name').textContent='Équipe introuvable';return}
  document.title=`${team.name} - FC Escalquens`;set('#team-name',team.name);set('#team-description',team.description);set('#player-count',team.player_count||'—');set('#team-level',team.level);
  document.querySelector('#player-label').textContent=team.group_name==='Féminines'||team.gender==='female'?'licenciées pratiquantes':'licenciés pratiquants';
  if(team.photo_key)document.querySelector('#team-photo').src=`/media/${team.photo_key}`;
  const memberMap=Object.fromEntries(members.map(member=>[member.id,member]));
  const staff=assignments.filter(item=>item.team_id===team.id&&item.active!==0).sort((a,b)=>a.display_order-b.display_order).map(item=>({...item,member:memberMap[item.member_id]})).filter(item=>item.member);
  document.querySelector('#team-staff').innerHTML=staff.map(item=>`<article>${item.member.photo_key?`<img src="/media/${esc(item.member.photo_key)}" alt="">`:''}<small>${roleLabels[item.role]||esc(item.role)}</small><h3>${esc(item.member.full_name)}</h3>${item.member.email?`<a href="mailto:${esc(item.member.email)}">${esc(item.member.email)}</a>`:''}${item.member.phone?`<a href="tel:${esc(item.member.phone)}">${esc(item.member.phone)}</a>`:''}</article>`).join('')||'<p>Encadrement à venir.</p>';
  const days=['','Lundi','Mardi','Mercredi','Jeudi','Vendredi','Samedi','Dimanche'],venueMap=Object.fromEntries(venues.map(venue=>[venue.id,venue]));
  document.querySelector('#team-training').innerHTML=sessions.filter(row=>row.team_id===team.id||row.category===team.category).map(row=>{const venue=venueMap[row.venue_id],query=venue?.latitude!=null&&venue?.longitude!=null?`${venue.latitude},${venue.longitude}`:venue?.address||row.address||venue?.name||row.venue,link=venue?.maps_url||`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(query)}`;return `<article><b>${days[row.weekday]}</b><span>${row.starts_at} - ${row.ends_at}</span><small>${esc(venue?.name||row.venue)}</small><a href="${link}" target="_blank">Itinéraire →</a></article>`}).join('')||'<p>Horaires à venir.</p>';
  const activeSeason=seasons.find(item=>item.active===1),names=[team.name,team.category,team.slug].filter(Boolean).map(value=>String(value).toLocaleLowerCase('fr'));
  const results=matches.filter(match=>match.home_score!=null&&match.away_score!=null&&(!activeSeason||!match.season_id||match.season_id===activeSeason.id)&&(match.team_id===team.id||names.some(name=>String(match.home_team).toLocaleLowerCase('fr').includes(name)||String(match.away_team).toLocaleLowerCase('fr').includes(name))||match.category===team.category)).sort((a,b)=>new Date(b.starts_at)-new Date(a.starts_at)).slice(0,5);
  document.querySelector('#team-results').innerHTML=results.map(match=>`<article><time>${new Date(match.starts_at).toLocaleDateString('fr-FR',{day:'numeric',month:'long'})}</time><small>${esc(match.competition)}</small><div><div class="match-team">${logo(match.home_logo_url,match.home_team)}<b>${esc(match.home_team)}</b></div><strong>${match.home_score} : ${match.away_score}</strong><div class="match-team">${logo(match.away_logo_url,match.away_team)}<b>${esc(match.away_team)}</b></div></div><span>${esc(match.venue||'Lieu à confirmer')}</span></article>`).join('')||'<p>Aucun résultat publié pour cette équipe.</p>';
}).catch(error=>{console.error(error);document.querySelector('#team-name').textContent='Informations indisponibles';document.querySelector('#team-description').textContent='La connexion aux données du club a échoué. Merci de réessayer dans quelques instants.'});
