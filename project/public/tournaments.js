const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const date=value=>value?new Date(`${value}T12:00:00`).toLocaleDateString('fr-FR',{day:'numeric',month:'long',year:'numeric'}):'';
let tournaments=[],filter='';
function categories(row){try{return Array.isArray(row.categories)?row.categories:JSON.parse(row.categories||'[]')}catch{return []}}
function draw(){const rows=tournaments.filter(row=>['published','open','finished'].includes(row.status)&&(!filter||categories(row).includes(filter)));document.querySelector('#tournaments-list').innerHTML=rows.map(row=>`<article class="tournament-card"><div class="tournament-date"><b>${date(row.starts_on)}</b>${row.ends_on&&row.ends_on!==row.starts_on?`<span>au ${date(row.ends_on)}</span>`:''}</div><div><small>${categories(row).map(esc).join(' · ')||'Toutes les catégories'}</small><h2>${esc(row.name)}</h2><p>${esc(row.summary)}</p><span class="tournament-place">${esc(row.venue)}</span><div class="tournament-actions">${row.registration_url?`<a href="${esc(row.registration_url)}" target="_blank" rel="noopener">S’inscrire →</a>`:''}${row.rules_key?`<a href="/media/${esc(row.rules_key)}" target="_blank">Voir le règlement</a>`:''}</div></div></article>`).join('')||'<div class="empty-state"><b>Les prochains tournois arrivent bientôt.</b><p>Les informations seront publiées ici dès leur confirmation.</p></div>'}
fetch('/api/tournaments').then(response=>response.json()).then(rows=>{tournaments=rows;const choices=[...new Set(rows.flatMap(categories))];document.querySelector('#tournament-filters').innerHTML=`<button class="active" data-category="">Tous</button>`+choices.map(choice=>`<button data-category="${esc(choice)}">${esc(choice)}</button>`).join('');document.querySelectorAll('[data-category]').forEach(button=>button.onclick=()=>{filter=button.dataset.category;document.querySelectorAll('[data-category]').forEach(item=>item.classList.toggle('active',item===button));draw()});draw()});
