const esc=value=>String(value??'').replace(/[<>"'&]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
window.fceHomeData=window.fceHomeData||fetch('/api/page/home').then(async response=>{const data=await response.json();if(!response.ok)throw new Error(data.error||`Accueil : ${response.status}`);return data});
window.fceHomeData.then(data=>{const teams=(data.teams||[]).slice().sort((a,b)=>String(a.name).localeCompare(String(b.name),'fr',{numeric:true,sensitivity:'base'})),matches=data.matches||[];const rail=document.querySelector('#teams-list');if(rail)rail.innerHTML=teams.map((team,index)=>`<a class="team-card tone-${index%4}" href="/equipes/fiche/?slug=${encodeURIComponent(team.slug)}"><span>${esc(team.group_name)}</span><b>${esc(team.name)}</b><small>${esc(team.level||team.category)}</small><i>→</i></a>`).join('');const root=document.querySelector('#matches-list');if(root)root.innerHTML=matches.map(match=>`<article><time>${new Date(match.starts_at).toLocaleDateString('fr-FR',{day:'numeric',month:'short'})}</time><div><b>${esc(match.category)}</b><span>${esc(match.home_team)} <em>vs</em> ${esc(match.away_team)}</span></div></article>`).join('')||'<p>Les prochaines rencontres arrivent bientôt.</p>';});
